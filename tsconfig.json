{
  "//": "Two independent programs share one vocabulary via type-only imports from server/domain: the browser app (tsconfig.app.json) and the Node server (tsconfig.server.json). `npm run typecheck` checks both; this file exists so editors pick up a project.",
  "files": [],
  "include": []
}
